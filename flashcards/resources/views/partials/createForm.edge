{{ csrfField() }}
<div class="field">
  @!component('components/input', { name: 'name', label: 'Nom', value: old('name') || (deck ? deck.name : '') })
</div>

<div class="field">
  @!component('components/input', { name: 'description', label: 'description', value: old('description') || ( deck ? deck.description : '') })
</div>
<a href="" type="submit">Enregistrer</a>
<button type="submit">Enregistrer</button>